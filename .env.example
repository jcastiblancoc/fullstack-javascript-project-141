# Example environment for CI and local setup
# Copy this file to the project root as .env when running locally: cp code/.env.example .env

NODE_ENV=development
PORT=3000
# Use in-memory sqlite for CI/tests
DB_FILE=:memory:
COOKIE_SECRET=change_this_secret
# Rollbar token left empty by default
ROLLBAR_ACCESS_TOKEN=
